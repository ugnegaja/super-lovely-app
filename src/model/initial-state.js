import {COMPLIMENT_IMAGE} from '../constants/constants';

const INITIAL_STATE = {
    currentSweetPhrase: ' ',
    currentInspirationPhrase: ' ',
    currentInspirationImage: COMPLIMENT_IMAGE,
    background: ['#ffffff00', '#ffffff00', '#ffffff00'],
};

export default INITIAL_STATE;
